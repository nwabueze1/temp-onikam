export type ServiceCardProps = {
  text: string;
  header: string;
  image: string;
  layoutClass?: string;
};

export type CardArrayProps = {
  image: string;
  text: string;
  header: string;
};
